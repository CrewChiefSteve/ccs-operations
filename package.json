{
  "name": "ccs-operations",
  "version": "0.1.0",
  "private": true,
  "description": "CCS Technologies Internal Operations Platform",
  "scripts": {
    "dev": "pnpm --filter @ccs/web dev",
    "dev:convex": "npx convex dev",
    "build": "pnpm --filter @ccs/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "dependencies": {
    "convex": "^1.31.7"
  }
}
