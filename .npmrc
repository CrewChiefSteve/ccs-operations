node-linker=hoisted
# Prevent React/React-DOM from being hoisted to root.
# apps/mobile uses React 18, apps/web uses React 19.
# Hoisting picks one version (18) which breaks the web build.
public-hoist-pattern[]=!react
public-hoist-pattern[]=!react-dom
public-hoist-pattern[]=*
